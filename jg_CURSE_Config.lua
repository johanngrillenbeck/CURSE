-- jg_CURSE_Config.lua
-- User configurations for CURSE scripts
-- Set default colors and other preferences here.

local Config = {
  colors = {
    FX = "faf06b",   -- FX track color (hex RGB)
    Bus = "906bfa",  -- Bus track color (hex RGB)
  },
}

return Config
